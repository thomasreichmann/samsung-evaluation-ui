import { ConvertedCurrency } from './converted-currency.model';

describe('ConvertedCurrency', () => {
  it('should create an instance', () => {
    expect(new ConvertedCurrency()).toBeTruthy();
  });
});
